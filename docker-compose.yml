services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # 開発時のみ：コードの変更を反映
      - ./ta_interview_briefing:/app/ta_interview_briefing
    restart: unless-stopped

